# .gitignore に追記（既にテンプレに入っていることが多い）
cat <<'EOF' >> .gitignore
log/*
tmp/*
storage/*
.env
EOF

# Credentials
/config/master.key
.env*
*.sqlite3
*.sqlite3-journal

# Coverage
/coverage/


# Ignore key files for decrypting credentials and more.
/config/*.key

